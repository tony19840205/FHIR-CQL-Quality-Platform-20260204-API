# 後端設計 - 超簡單版（給非專業人士）

> **目標**: 用最簡單的方式建立後端，計算醫療品質指標的分子分母  
> **技術**: Node.js（就是JavaScript在伺服器上跑）  
> **時間**: 30分鐘搞定

---

## 📦 第一步：安裝Node.js（如果還沒裝）

1. 下載：https://nodejs.org/zh-tw/download/
2. 點兩下安裝檔，一直按「下一步」
3. 完成！

---

## 🚀 第二步：建立後端資料夾

```powershell
# 在你的專案旁邊建立新資料夾
cd "c:\Users\tony1\Desktop\UI UX- 20260108"
mkdir backend
cd backend

# 初始化專案
npm init -y

# 安裝需要的套件（只要2個）
npm install express axios
```

**說明**：
- `express` = 讓你建立API的工具（超簡單）
- `axios` = 用來呼叫FHIR服務器的工具

---

## 💻 第三步：建立後端程式（複製貼上）

### 檔案1：`server.js` （主程式）

```javascript
// ========== 醫療品質指標後端 API ==========
// 用途：計算分子分母，減輕前端負擔

const express = require('express');
const axios = require('axios');
const app = express();
const PORT = 3000;

// 允許跨域（前端才能呼叫）
app.use((req, res, next) => {
    res.header('Access-Control-Allow-Origin', '*');
    res.header('Access-Control-Allow-Headers', 'Content-Type');
    next();
});

// 解析JSON
app.use(express.json());

// ========== 健康檢查（測試用） ==========
app.get('/health', (req, res) => {
    res.json({ 
        status: 'OK', 
        message: '後端運作中',
        time: new Date().toISOString() 
    });
});

// ========== 核心API：計算指標 ==========
app.post('/api/calculate', async (req, res) => {
    try {
        const { indicatorId, fhirServer, quarter } = req.body;
        
        console.log(`📊 開始計算指標: ${indicatorId}, 季度: ${quarter}`);
        
        // 根據不同指標呼叫對應的計算函數
        let result;
        
        if (indicatorId === 'indicator-01') {
            result = await calculateIndicator01(fhirServer, quarter);
        } else if (indicatorId === 'indicator-02') {
            result = await calculateIndicator02(fhirServer, quarter);
        } else {
            result = await calculateGeneric(fhirServer, quarter);
        }
        
        console.log(`✅ 計算完成:`, result);
        res.json(result);
        
    } catch (error) {
        console.error('❌ 計算失敗:', error.message);
        res.status(500).json({ error: error.message });
    }
});

// ========== 指標01：門診注射劑使用率 ==========
async function calculateIndicator01(fhirServer, quarter) {
    // 步驟1: 查詢門診資料
    const encountersUrl = `${fhirServer}/Encounter?class=AMB&status=finished&_count=2000`;
    const encountersRes = await axios.get(encountersUrl);
    const encounters = encountersRes.data.entry || [];
    
    console.log(`  📦 查到 ${encounters.length} 個門診`);
    
    // 步驟2: 查詢用藥資料
    const medsUrl = `${fhirServer}/MedicationRequest?status=completed&_count=2000`;
    const medsRes = await axios.get(medsUrl);
    const medications = medsRes.data.entry || [];
    
    console.log(`  💊 查到 ${medications.length} 個用藥`);
    
    // 步驟3: 建立有效門診ID集合
    const validEncounterIds = new Set();
    for (const encEntry of encounters) {
        const enc = encEntry.resource;
        const encounterType = enc.class?.code;
        
        // 排除急診、門診手術
        if (encounterType !== '02' && encounterType !== '03') {
            validEncounterIds.add(enc.id);
        }
    }
    
    // 步驟4: 計算分子分母
    let injectionCount = 0;  // 分子
    let totalDrugCount = 0;  // 分母
    
    for (const medEntry of medications.entry) {
        const med = medEntry.resource;
        
        // 檢查是否屬於有效門診
        const encounterRef = med.encounter?.reference;
        if (!encounterRef) continue;
        
        const encounterId = encounterRef.split('/').pop();
        if (!validEncounterIds.has(encounterId)) continue;
        
        totalDrugCount++;
        
        // 判斷是否為注射劑（10碼且第8碼='2'）
        const codings = med.medicationCodeableConcept?.coding || [];
        for (const coding of codings) {
            const code = coding.code || '';
            if (code.length === 10 && code.charAt(7) === '2') {
                injectionCount++;
                break;
            }
        }
    }
    
    // 步驟5: 計算比率
    const rate = totalDrugCount > 0 
        ? ((injectionCount / totalDrugCount) * 100).toFixed(2) 
        : '0.00';
    
    return {
        numerator: injectionCount,
        denominator: totalDrugCount,
        rate: rate,
        quarter: quarter
    };
}

// ========== 指標02：門診抗生素使用率 ==========
async function calculateIndicator02(fhirServer, quarter) {
    // 查詢門診
    const encountersUrl = `${fhirServer}/Encounter?class=AMB&status=finished&_count=2000`;
    const encountersRes = await axios.get(encountersUrl);
    const encounters = encountersRes.data.entry || [];
    
    // 查詢用藥
    const medsUrl = `${fhirServer}/MedicationRequest?status=completed&_count=2000`;
    const medsRes = await axios.get(medsUrl);
    const medications = medsRes.data.entry || [];
    
    // 建立有效門診ID
    const validEncounterIds = new Set();
    for (const encEntry of encounters) {
        const enc = encEntry.resource;
        const encounterType = enc.class?.code;
        if (encounterType !== '02' && encounterType !== '03') {
            validEncounterIds.add(enc.id);
        }
    }
    
    // 計算分子分母
    let antibioticCount = 0;  // 分子（抗生素）
    let totalDrugCount = 0;   // 分母
    
    for (const medEntry of medications.entry) {
        const med = medEntry.resource;
        
        const encounterRef = med.encounter?.reference;
        if (!encounterRef) continue;
        
        const encounterId = encounterRef.split('/').pop();
        if (!validEncounterIds.has(encounterId)) continue;
        
        totalDrugCount++;
        
        // 判斷是否為抗生素（ATC碼前3碼='J01'）
        const codings = med.medicationCodeableConcept?.coding || [];
        for (const coding of codings) {
            const atcCode = coding.code || '';
            if (atcCode.startsWith('J01')) {
                antibioticCount++;
                break;
            }
        }
    }
    
    const rate = totalDrugCount > 0 
        ? ((antibioticCount / totalDrugCount) * 100).toFixed(2) 
        : '0.00';
    
    return {
        numerator: antibioticCount,
        denominator: totalDrugCount,
        rate: rate,
        quarter: quarter
    };
}

// ========== 通用指標計算（其他39項） ==========
async function calculateGeneric(fhirServer, quarter) {
    // 簡化版：直接回傳示範資料
    return {
        numerator: 0,
        denominator: 0,
        rate: '0.00',
        quarter: quarter,
        message: '尚未實作此指標的後端運算'
    };
}

// ========== 啟動伺服器 ==========
app.listen(PORT, () => {
    console.log('');
    console.log('╔════════════════════════════════════════════════════╗');
    console.log('║   醫療品質指標後端 API - 已啟動                   ║');
    console.log('╠════════════════════════════════════════════════════╣');
    console.log(`║   🌐 網址: http://localhost:${PORT}                  ║`);
    console.log('║   📊 計算API: POST /api/calculate                 ║');
    console.log('║   💚 健康檢查: GET /health                        ║');
    console.log('╚════════════════════════════════════════════════════╝');
    console.log('');
    console.log('✅ 後端運作中，按 Ctrl+C 停止');
    console.log('');
});
```

---

## ▶️ 第四步：啟動後端

```powershell
# 在 backend 資料夾裡執行
node server.js
```

**你應該會看到**：
```
╔════════════════════════════════════════════════════╗
║   醫療品質指標後端 API - 已啟動                   ║
╠════════════════════════════════════════════════════╣
║   🌐 網址: http://localhost:3000                  ║
║   📊 計算API: POST /api/calculate                 ║
║   💚 健康檢查: GET /health                        ║
╚════════════════════════════════════════════════════╝

✅ 後端運作中，按 Ctrl+C 停止
```

---

## 🧪 第五步：測試後端

### 方法1：用瀏覽器測試（最簡單）

1. 開啟瀏覽器
2. 輸入：`http://localhost:3000/health`
3. 看到 `{"status":"OK"}` 就成功了！

### 方法2：用PowerShell測試

```powershell
# 測試健康檢查
curl http://localhost:3000/health

# 測試計算API（指標01）
$body = @{
    indicatorId = "indicator-01"
    fhirServer = "https://hapi.fhir.org/baseR4"
    quarter = "2025-Q4"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/calculate" -Method POST -Body $body -ContentType "application/json"
```

**應該會看到**：
```json
{
  "numerator": 12,
  "denominator": 158,
  "rate": "7.59",
  "quarter": "2025-Q4"
}
```

---

## 🔗 第六步：前端連接後端

修改前端的 `js/quality-indicators.js`：

```javascript
// 在檔案最前面加上這個設定
const BACKEND_API_URL = 'http://localhost:3000';  // 後端網址

// 修改 executeQuery 函數
async function executeQuery(indicatorId) {
    try {
        // ========== 新增：檢查是否使用後端 ==========
        const useBackend = window.USE_BACKEND_API === true;
        
        if (useBackend) {
            // 呼叫後端API
            console.log('✅ 使用後端計算');
            const response = await fetch(`${BACKEND_API_URL}/api/calculate`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    indicatorId: indicatorId,
                    fhirServer: window.fhirConnection.serverUrl,
                    quarter: getCurrentQuarter()
                })
            });
            
            const result = await response.json();
            
            // 更新顯示
            updateIndicatorDisplay(`${indicatorId.replace('indicator-', 'ind')}Rate`, `${result.rate}%`);
            
            return result;
        } else {
            // 原本的前端計算（保留）
            console.log('✅ 使用前端計算');
            return await queryInBrowser(indicatorId);
        }
        
    } catch (error) {
        console.error('查詢失敗:', error);
        return null;
    }
}
```

---

## 🎮 第七步：切換前端/後端模式

在瀏覽器的開發者工具（F12）Console輸入：

```javascript
// 啟用後端模式
window.USE_BACKEND_API = true;

// 切回前端模式
window.USE_BACKEND_API = false;
```

或在HTML加一個按鈕：

```html
<!-- 在 quality-indicators.html 的按鈕區加上 -->
<button onclick="toggleBackend()" class="btn btn-info">
    <i class="fas fa-server"></i> <span id="backendModeText">啟用後端運算</span>
</button>

<script>
function toggleBackend() {
    window.USE_BACKEND_API = !window.USE_BACKEND_API;
    const text = window.USE_BACKEND_API ? '切回前端運算' : '啟用後端運算';
    document.getElementById('backendModeText').textContent = text;
    
    const mode = window.USE_BACKEND_API ? '後端' : '前端';
    alert(`已切換到 ${mode} 運算模式`);
}
</script>
```

---

## 📊 效能比較

| 模式 | 計算位置 | 速度 | 網路流量 | 適用場景 |
|------|----------|------|----------|----------|
| **前端** | 瀏覽器 | 慢（3-5秒） | 大（下載所有資料） | 小數據（<1000筆） |
| **後端** | 伺服器 | 快（<1秒） | 小（只傳結果） | 大數據（>5000筆） |

---

## 🚢 部署到真實伺服器（可選）

### 選項1：Heroku（免費，最簡單）

1. 註冊 https://www.heroku.com
2. 安裝 Heroku CLI
3. 執行：
```powershell
cd backend
heroku login
heroku create my-quality-backend
git init
git add .
git commit -m "Initial commit"
git push heroku master
```

### 選項2：Railway（免費，超簡單）

1. 註冊 https://railway.app
2. 點「New Project」→「Deploy from GitHub」
3. 選擇你的專案
4. 完成！自動部署

### 選項3：自己的電腦（測試用）

```powershell
# 讓後端一直跑（即使關閉PowerShell）
npm install -g pm2
pm2 start server.js --name quality-backend
pm2 save
pm2 startup
```

---

## 🔧 常見問題

### Q1: 啟動後端時出現 `Error: Cannot find module 'express'`？
**A**: 執行 `npm install express axios`

### Q2: 前端無法連接後端，出現CORS錯誤？
**A**: 確認後端的CORS設定（已包含在 `server.js` 中）

### Q3: 想加更多指標的計算？
**A**: 複製 `calculateIndicator01` 函數，改名為 `calculateIndicator03`，修改裡面的邏輯

### Q4: 如何讓後端跑得更快？
**A**: 加上快取（Cache）：
```javascript
// 安裝 node-cache
npm install node-cache

// 在 server.js 最上面加上
const NodeCache = require('node-cache');
const cache = new NodeCache({ stdTTL: 600 }); // 快取10分鐘

// 在計算前先查快取
const cacheKey = `${indicatorId}-${quarter}`;
const cached = cache.get(cacheKey);
if (cached) {
    console.log('✅ 從快取讀取');
    return res.json(cached);
}

// 計算完後存快取
cache.set(cacheKey, result);
```

---

## 📁 最終資料夾結構

```
UI UX- 20260108/
├── backend/                    ← 新建的後端
│   ├── server.js              ← 主程式
│   ├── package.json
│   └── node_modules/
│
├── js/
│   └── quality-indicators.js  ← 修改過的前端
│
└── quality-indicators.html
```

---

## ✅ 完成檢查清單

- [ ] Node.js已安裝
- [ ] backend資料夾已建立
- [ ] `npm install express axios` 已執行
- [ ] `server.js` 已建立
- [ ] `node server.js` 可以啟動
- [ ] 瀏覽器測試 `http://localhost:3000/health` 成功
- [ ] 前端可以切換後端模式

---

## 🎯 總結

### 你現在有什麼？
1. ✅ 一個超簡單的後端API
2. ✅ 可以計算指標01和02
3. ✅ 前端可以隨時切換模式
4. ✅ 總共只有1個檔案（`server.js`）

### 下一步？
- **如果測試資料夠用** → 繼續用前端，不需要後端
- **如果資料量變大** → 啟用後端模式
- **如果要加更多指標** → 複製 `calculateIndicator01` 改名

### 花多少時間？
- **建立**: 10分鐘（複製貼上）
- **測試**: 5分鐘
- **部署**: 15分鐘（可選）

**總計：30分鐘搞定！** 🎉

---

**文件版本**: v1.0  
**最後更新**: 2026-01-15  
**難度**: ⭐☆☆☆☆（超簡單）
